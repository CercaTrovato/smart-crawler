# smart-crawler 全面质检报告（2026-07-05）

> 质检对象：Python 采集系统 **smart-crawler**（升级链抓取 + LLM 抽取 + 严格白名单信封产物），标准仓库 `E:\smart-crawler`。
> 工作区：worktree `E:\AIworkspace\claude_workspace\smart-crawler-qc`，分支 `qc-20260705`（基线 main `0d6b936`）。
> 方法：5 维 subagent fan-out 深审 + 主 agent 独立复核（读全部 1497 行）→ 交叉去重 → 对抗验证 → karpathy 外科手术式修复 → 全量真跑取证。
> **合并回 main / 推 GitHub / 同步到留学项目拷贝由 Owner 定。**

## 一、结论

- 挖出并修复 **1 个 🔴（红线相关）+ 21 个 🟡（机制确定）+ 7 个建议项**，覆盖全部 8 个源文件 + 文档。
- 改动 **10 文件 +203/-77 行**（含大量注释），无扩面、无顺手重构。
- **QC 回归测试 39/39 全绿**；worker 隔离回归全绿；smoke/samedomain(同域并发)/validate(跨域升级链)/resume/边界 全量真跑通过。
- **红线不变量全部保持**：`contains_privacy` 恒 false、`data_confidence` 只 high|medium|low、绝不产控制字段、不抓 PII、遇验证码/封页停不破解。
- 另砍掉 9 处 subagent/主 agent 曾疑但经验证的**误报**（见 §五）。

## 二、🔴 必修（已修）

| ID | 位置 | 问题 | 根因 | 修法 | 证据 |
|----|------|------|------|------|------|
| **F1** | output.py `_clean_item` | 白名单只过滤"键"不过滤"值"：LLM 让白名单字段值为 dict/list 时，其内部 `is_simulated`/`status`/`review_status` 随 `json.dump` 落盘，`_assert_no_control_fields` 只查顶层键查不到——文本性突破"绝不产控制字段"红线 | extract 层对 LLM 输出仅 `json.loads` 无类型强制；output 是唯一净化关口却漏了值层 | `_clean_item` 值层标量化：非标量/dict 丢弃，list 仅留标量元素；`_assert_no_control_fields` 改**递归**扫嵌套键 | QC 测试 `F1 嵌套控制字段不落盘` red→green |

## 三、🟡 必修（已修，21 项）

### output.py（产物契约）
| ID | 位置 | 问题 → 修法 | 证据 |
|----|------|------|------|
| F2 | `_to_num` | 区间 `"12,500-15,000"`→`1250015000`(错误巨数) → 取第一个数字段 | `_to_num区间取下界` |
| F3 | `_to_num` | `"7.5 (6.0)"`→None 丢 UK 常见 IELTS → 同 F2 取括号外首数 | `_to_num总分(小分)` |
| F5 | `assert`×2 | `assert` 在 `python -O` 被剥离，§11-C"报错不投喂"失效 → 改 `if+raise`，且递归查嵌套 | 语法+递归验证 |
| F6 | name 循环/`degree` | `degree_name` 误当项目名、`degree` fuzzy 误取 `degree_requirement` → name 排除 degree、degree 去 fuzzy | `degree_name不当项目名`/`degree不误取requirement` |
| F7 | missing_fields 合并 | 未校验元素类型，控制字段字样二次泄漏 → `isinstance(f,str)` 过滤 | `missing_fields仅字符串` |
| F4 | official_url/website | exact 含宽泛键 `url`/`website`，LLM 幻觉 url 顶掉可信 final_url → 去宽泛键（**精准修法**，不破坏 wikipedia 官网场景） | `幻觉url不顶final_url`+3 不回归 |
| F8 | `_normalize_duration` | 小数 months 回溯 `"1.5 months"`→`"5 个月"` → 正则支持小数 | `duration小数months` |
| F10 | `build_envelope` | 非 dict extraction 不发降级告警 → 追加 extract_degraded | `非dict抽取有降级告警` |
| F18 | `write_payload` | 文件名 `safe[:80]-秒` 不唯一 + 非原子直写 → 并入 task_key 短哈希 + tmp+os.replace | samedomain 8 产物 hash 各异、无残留 .tmp |

### scoring.py（打分）
| ID | 位置 | 问题 → 修法 | 证据 |
|----|------|------|------|
| F11 | `html_to_text` | 数字实体 `chr(N)` N>0x10FFFF 抛 ValueError/OverflowError 冒泡→误标 needs_manual → 改用 `html.unescape`（含 hex/越界安全降级） | `畸形数字实体不崩` |
| F13 | `looks_blocked` | 裸词 "cloudflare" 等误判良性 200 页 → 仅短正文(<1500)生效 + 移除裸 cloudflare | `长正文cloudflare不误判`+真封页仍识别 |
| F14 | `_RE_TAG` | 长无闭合 `<` 串 O(n²) 卡死 worker(1.6MB≈13min) → possessive 限长 `<[^>]{0,4096}+>` + 入口截断 | 实测 2.1MB：13min→**1.7s** |

### escalate.py（升级/记账）
| ID | 位置 | 问题 → 修法 | 证据 |
|----|------|------|------|
| F12 | refund 条件 | firecrawl 非-200(402/429/500) httpx 不抛异常、不置 error → credit 少退、预算幽灵消耗→误拒逃生档 → 改 `not r.get("ok") or fallback=="stealthy"` | validate UCL firecrawl 后 credit 守恒剩 200 |

### scheduler.py（并发/调度）
| ID | 位置 | 问题 → 修法 | 证据 |
|----|------|------|------|
| F15 | 排序行 | `t["url"]` 下标，缺 url 在 gather 后抛 KeyError 丢**整批** → `_safe_task_key` | `缺url target不崩整批` |
| F16 | worker except | except 块内 `target.get` 对非 dict 二次抛异常击穿隔离 → 不依赖 target 结构 | `None target不崩整批` |
| F21 | `mark_done` | `--resume=False` 仍无条件写 state.json→下次 resume 读到非预期完成 → 判 `self.enabled` | samedomain 无 --resume→state 不存在；resume 幂等两跑 |

### config.py / extract.py / run.py
| ID | 位置 | 问题 → 修法 | 证据 |
|----|------|------|------|
| F23 | config 数值字段 | `llm.timeoutMs/maxRetry`唯独不 clamp、`minDelayMs/backoffMs` 裸 `int()`→非数值抛异常冒泡整批 → `_safe_int` 统一兜底 | `llm规范数值`/`非数值politeness不崩` |
| F24 | `_extract_codex` | mkdtemp+schema 落盘在 try 外→失败 tmpdir 泄漏+异常冒泡 → 纳入 try/finally | 结构性 |
| F25 | `_parse_json_loose` | 返回非 dict(null/裸标量/数组)无 `__extract_error__`，降级不可见 → isinstance 校验 | `裸null/标量/数组返错误标记` |
| F26 | config openai-compat | 未装 httpx→ImportError 冒泡 → config 验 httpx（对称 scrapling） | — |
| F28 | `_extract_codex` | FileNotFoundError 混淆 codex 缺失 vs out_file → 先 `which` 判空 + 附 rc/stderr | — |
| F30 | run/config 读 JSON | 坏/缺 targets/config 抛栈 exit1 → try→友好 exit2 | 边界 7/7 exit=2 |
| F31 | config clamp | 上限 64/16/8 超 §11-D 契约 → 对齐 32/4/2 | `clamp` 999→32、browserCap4、firecrawlCap2 |
| F17 | run targets 校验 | scheme-less URL→host 空→限流桶塌缩防封失效 → 补 scheme 校验/自动补 https:// | `scheme非法 exit2` |
| F33 | run-report | 表格未转义 `\|` 破表 → `_cell` 转义 | — |
| F35 | 读 JSON | 手写文件 BOM 解析失败 → `utf-8-sig` | 含边界 |
| F36 | run 入口 | 控制台中文乱码 → stdout/stderr reconfigure utf-8 | 冒烟诊断中文正常 |
| F29 | setup.md/requirements.txt | `-m scrapling install` 坏命令(无 __main__) → 改 console script | 文档 |

## 四、建议修（已修）
- **F32**（extract.py + config/文档）：`NO_PROXY='*'` 误伤远程 openai-compat 抽取 → 增 `proxyEnv`(默认 LLM_PROXY) 显式代理逃生阀。
- F27（codex returncode/stderr 诊断）、F22a（HostLimiter 可选 monotonic——**未改**，见下）等并入或降级。

## 五、报告不改（设计取舍，建议 Owner 定夺）
- **F9**：programme 缺 `university_id` 只记 missing 不 blocked，import_recommendation 仍 recommend_manual_review（no_parent warning 已给信号；改 block 会翻转所有无 university_id 的 programme）。
- **F20**：resume 把 needs_manual 也标 done，`--resume` 不重试瞬时失败（§11-E 字面"产物落盘即 done"合规）。
- **F19**：`_normalize_url` 归一不含 query 排序/端口/scheme 归并（去重口径需业务定）。
- **F22b**：`mark_done` 每次全量写盘 O(N²)（§11-E/K 已弃 sqlite 的已知代价）；HostLimiter `time.time()` 墙钟（NTP 回拨极端情形，`max(now,_last)` 已部分保护）。

## 六、已砍误报（对抗验证证伪，避免无谓改动）
timeout 单位不一致（各库约定均生效）· HostLimiter 15ms 间隔（Windows 定时器量化，算法正确）· CreditGate 类双扣/负/超还（原子无缺陷）· 队列消费者饥饿/死锁（无）· `_atomic_write_json` Windows os.replace/tmp+pid（正确）· firecrawl httpx proxy 受 NO_PROXY 误伤（参数式不受影响，httpx 源码验证）· `contains_privacy`被置true/LLM自报置信度（进不了产物/确被丢弃）· `School of Data Science MSc`被误杀/`gre`匹配deGREe（安全）· DomainMemory.remember 竞态（同步无 await 不交错）。

## 七、全量测试证据

| 测试 | 结果 |
|------|------|
| `_test_qc_regression.py`（新增补测，覆盖 18 类修复） | **39/39 全绿** |
| `_test_worker_isolation.py`（原回归） | 全绿（5/5 隔离有效） |
| 冒烟 smoke（2 目标） | 2/2 usable；信封白名单/枚举/CJK/duration/官网/contains_privacy 全对；F18/F36 生效 |
| **同域并发 samedomain**（8×wikipedia 并发8） | 8/8 usable；HostLimiter 同域限流生效（10s 内分批发射）；credit 守恒；domain-memory 单点；F21 不越权写 state；hash 无碰撞、无残留 tmp |
| **跨域升级链 validate**（5 目标并发4） | 4 usable(官网 high)/1 needs_manual(UCL 全档 403→诚实标)；升级链 http-tls→browser→firecrawl 完整；无 key 降级 stealthy；**F12 credit 守恒剩 200** |
| resume 幂等（smoke 连跑两次） | 第1次抓+写 state，第2次全跳过（0.2s，"resume 跳过 2"） |
| 边界（空/坏 targets/坏 config/scheme/clamp/缺后端） | 全部友好 exit=2 不抛栈；clamp 999→32/0→1 |

## 八、新增测试
- `_test_qc_regression.py`：39 项断言，纯逻辑 + 轻量 asyncio mock，不走真实网络。覆盖 F1/F2/F3/F5/F6/F7/F8/F10/F11/F13/F15/F16/F21/F23/F25/F31 及多条不回归断言。可随 `_test_worker_isolation.py` 一并作为回归门禁。

## 九、独立验收结论（独立 subagent 对抗审查修复质量）

独立 reviewer 逐一对抗验证 25+ 处修复（构造边界输入喂纯函数 + 独立跑回归）：

- **确认正确**：F1（标量化堵真洞，`strength_subjects` 等合法 list 不误删）、**F12（本轮质量最高**，五路径记账全对、诊断精准）、F15/F16（排序 key 一致性确认、隔离彻底）、F5（raise 守红线、递归无环）、F31（默认配置不受影响）、F13（真封页仍识别）、F2/F3/F4/F6/F7/F8/F10/F18/F21/F23–F36——逐条测试通过。
- **发现并修复 1 个回归**：**F11** 用 `html.unescape` 整段替换手写解码，顺手把 `&nbsp;` 从"普通空格"改成 `U+00A0`(\xa0)，下游 `_RE_WS=[ \t]+` 不匹配 \xa0 → 多词关键词 `"entry requirements"` 子串失配 → 好页误判 not usable → 多余升级/误标 needs_manual（reviewer venv 实证）。**已最小收口**：`_RE_WS` 放宽为 `[^\S\n]+` 塌缩所有非换行空白 + 补 `&nbsp;` 回归用例；复测 **40/40 全绿**。
- **备案（低/极低，未改）**：F14 possessive 限长致超长 data-URI 标签残留抬高 text_len（与 F13 len<1500 护栏理论耦合，但此类页通常 403 走状态码分支，极边缘）；F18 tmp 用 pid（path 已含 hash+时间戳，碰撞不可达）；F33 `_cell` 不转义换行（reason 经 `[:150]` 截断、极少含换行）。

**总评**：红线全部守住（F1+F5 反而强化"绝不产控制字段"）；karpathy 纪律整体外科手术、可溯源、无扩面，唯一越界即 F11 已收口。25+ 处修复中 F12 质量最高，F11 是唯一需返工者（已修）。
